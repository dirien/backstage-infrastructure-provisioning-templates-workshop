kind: ClusterSecret
apiVersion: clustersecret.io/v1
metadata:
  name: global-secret
matchNamespace:
    - "backstage"
data:
  valueFrom:
    secretKeyRef:
      name: all-credentials
      namespace: argocd
